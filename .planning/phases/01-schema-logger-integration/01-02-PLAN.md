---
phase: 01-schema-logger-integration
plan: 02
type: tdd
wave: 2
depends_on: ["01-01"]
files_modified:
  - core/order_manager.py
  - runner.py
  - tests/test_order_manager_logging.py
autonomous: true
requirements: [INTEG-01, INTEG-02, INTEG-03, INTEG-04, INTEG-05, INTEG-06]
must_haves:
  truths:
    - "OrderManager accepts md_logger/strategy_name/account_name with defaults — existing callers still work unchanged"
    - "Each of buy, sell, short_sell, buy_to_cover calls md_logger.log_order after a successful submission"
    - "close_position does NOT call log_order (no Order object returned)"
    - "runner.py constructs MotherDuckLogger only when MOTHERDUCK_TOKEN is present"
    - "runner.py snapshots positions/portfolio and polls fills after run_cron via try/finally"
    - "No file under strategies/ is modified"
  artifacts:
    - path: "core/order_manager.py"
      provides: "OrderManager with md_logger param and log_order calls in 4 order methods"
      contains: "md_logger"
    - path: "runner.py"
      provides: "Conditional MotherDuckLogger construction and snapshot/fill-poll try/finally"
      contains: "MOTHERDUCK_TOKEN"
    - path: "tests/test_order_manager_logging.py"
      provides: "Tests proving log_order is called for 4 methods and NOT for close_position, plus backward-compat"
      contains: "def test_buy_logs_order"
  key_links:
    - from: "core/order_manager.py"
      to: "md_logger.log_order"
      via: "if self.md_logger: call after submit_order succeeds"
      pattern: "self\\.md_logger\\.log_order"
    - from: "runner.py"
      to: "core.motherduck_logger.MotherDuckLogger"
      via: "conditional import + construct when token present"
      pattern: "MOTHERDUCK_TOKEN"
    - from: "runner.py"
      to: "md_logger.snapshot_positions / snapshot_portfolio / update_fill"
      via: "try/finally wrapping run_cron"
      pattern: "finally"
---

<objective>
Wire the `MotherDuckLogger` (from Plan 01) into `OrderManager` and `runner.py` without touching any strategy file. `OrderManager` gains three new optional constructor params (`md_logger=None`, `strategy_name=""`, `account_name=""`) and calls `md_logger.log_order()` after each successful submission in the four submitting order methods. `runner.py` constructs the logger only when `MOTHERDUCK_TOKEN` is present, passes it (plus strategy/account names) to `OrderManager`, and wraps `run_cron`/`run_stream` in a `try/finally` that snapshots positions + portfolio and polls Alpaca for fills on exit.

Per RESEARCH.md Pitfall 1: `run_cron()` runs `BlockingScheduler.start()` forever, so the snapshot/fill-poll block only fires on process exit (SIGINT/SIGTERM). The `try/finally` matches the INTEG-04/05 wording ("after run_cron returns") and keeps `scheduler.py` untouched.

Purpose: Every order from every strategy is logged, and snapshots are written after each run — all changes confined to `core/` and `runner.py`.
Output: edits to `core/order_manager.py` and `runner.py`; new `tests/test_order_manager_logging.py`.
</objective>

<artifacts_this_phase_produces>
Symbols created/modified by this plan (new — exclude from drift verification):

- `OrderManager.__init__` new params: `md_logger=None`, `strategy_name=""`, `account_name=""`
- New `self.md_logger`, `self.strategy_name`, `self.account_name` attributes on `OrderManager`
- `md_logger.log_order(...)` call sites inside `buy`, `sell`, `short_sell`, `buy_to_cover`
- runner.py: `import os`; conditional `from core.motherduck_logger import MotherDuckLogger`; `token`/`md_logger`/`account_name` locals; try/finally snapshot + fill-poll block
- `tests/test_order_manager_logging.py` (new file) with tests: `test_backward_compat_no_md_logger`, `test_buy_logs_order`, `test_sell_logs_order`, `test_short_sell_logs_order`, `test_buy_to_cover_logs_order`, `test_close_position_does_not_log`, `test_log_order_receives_strategy_and_account`
- `_RecordingLogger` test stub (records log_order calls)
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-schema-logger-integration/01-RESEARCH.md
@.planning/phases/01-schema-logger-integration/01-PATTERNS.md
@.planning/phases/01-schema-logger-integration/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests, then add md_logger to OrderManager (4 methods + backward compat)</name>
  <files>tests/test_order_manager_logging.py, core/order_manager.py</files>
  <read_first>
    - core/order_manager.py (the file being edited — current __init__ at lines 6-8, the 4 submitting methods buy/sell/short_sell/buy_to_cover at lines 16-82, close_position at lines 84-91, cancel_all_orders at 92-97)
    - tests/test_live_execution.py (analog: `MockClient`/`_MockTrading` at lines 44-79, `_NullLogger` at 81-85, `_build` helper at 88-100 — reuse these construction and stub patterns)
    - .planning/phases/01-schema-logger-integration/01-PATTERNS.md (core/order_manager.py edit section lines 156-196: exact insertion point and backward-compat signature; tests/test_order_manager_logging.py section lines 327-349)
    - .planning/phases/01-schema-logger-integration/01-RESEARCH.md (Pattern 2, Pitfall 4 strategy_name/account_name injection, close_position anti-pattern, "Try/except with return None" shared pattern)
  </read_first>
  <action>
    First write tests/test_order_manager_logging.py (RED). Reuse the `MockClient`/`_MockTrading` style from tests/test_live_execution.py: `submit_order` returns a fake order with an `.id` attribute. Add a `_RecordingLogger` stub class exposing `log_order(self, order, strategy_name, account_name)` that appends `(order, strategy_name, account_name)` to a `self.calls` list. Construct OrderManager directly (no strategy package needed): `OrderManager(client=MockClient(), logger=_NullLogger(), md_logger=rec, strategy_name="stat_arb", account_name="stat_arb")`.

    Write these tests (failing now because OrderManager has no md_logger param — RED):
    - `test_backward_compat_no_md_logger`: `OrderManager(client=MockClient(), logger=_NullLogger())` constructs and `.buy("AAPL", 1)` returns the mock order without error (INTEG-01). This is the critical backward-compat guard.
    - `test_buy_logs_order`: after `om.buy("AAPL", 1)`, assert `len(rec.calls) == 1` and `rec.calls[0][0]` is the returned order.
    - `test_sell_logs_order`: after `om.sell("AAPL", 1)`, assert one recorded call.
    - `test_short_sell_logs_order`: after `om.short_sell("AAPL", 1)`, assert one recorded call.
    - `test_buy_to_cover_logs_order`: after `om.buy_to_cover("AAPL", 1)`, assert one recorded call.
    - `test_close_position_does_not_log`: after `om.close_position("AAPL")`, assert `len(rec.calls) == 0` (close_position returns no Order — INTEG-02 explicitly excludes it from log_order).
    - `test_log_order_receives_strategy_and_account`: after `om.buy("AAPL", 1)`, assert `rec.calls[0][1] == "stat_arb"` and `rec.calls[0][2] == "stat_arb"` (Pitfall 4: names injected via __init__).

    Then edit core/order_manager.py to make them pass (GREEN):
    - Change `__init__(self, client, logger)` to `__init__(self, client, logger, md_logger=None, strategy_name="", account_name="")`; set `self.md_logger`, `self.strategy_name`, `self.account_name`. Existing call sites that pass only `client=`/`logger=` keep working (INTEG-01).
    - In each of `buy`, `sell`, `short_sell`, `buy_to_cover`: immediately after the existing `self.logger.info(f"... submitted order_id={order.id}")` line and before `return order`, insert `if self.md_logger: self.md_logger.log_order(order, self.strategy_name, self.account_name)`. The call stays INSIDE the existing `try` block so a successful submission is logged; if log_order itself raises, let it propagate (fail loud on logging bugs) per PATTERNS.md shared pattern (INTEG-02).
    - Do NOT add any log_order call to `close_position` (no Order object) or `cancel_all_orders`. Add a one-line comment in `close_position` noting it is intentionally not logged.

    Touch only core/order_manager.py and the new test file. Do not edit any strategies/ file (INTEG-06).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_order_manager_logging.py tests/test_live_execution.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_order_manager_logging.py -q` — all 7 tests pass (GREEN)
    - `.venv/bin/python -m pytest tests/test_live_execution.py -q` — still green (backward compatibility proven; existing `OrderManager(client=client, logger=_NullLogger())` call at line 98 unchanged and working)
    - `grep -c 'self.md_logger.log_order' core/order_manager.py` returns exactly 4 (buy, sell, short_sell, buy_to_cover only)
    - `core/order_manager.py` `__init__` signature contains `md_logger=None, strategy_name="", account_name=""`
    - `close_position` contains no `log_order` call and has a comment explaining why
    - `git diff --name-only HEAD strategies/` is empty (INTEG-06)
  </acceptance_criteria>
  <done>OrderManager logs every successful order from the 4 submitting methods with strategy/account context, excludes close_position, and remains backward compatible; 7 new tests green and existing suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire MotherDuckLogger into runner.py with conditional construction and try/finally snapshots</name>
  <files>runner.py</files>
  <read_first>
    - runner.py (the file being edited — imports at lines 1-13, OrderManager construction at lines 54-62, cron/stream dispatch at lines 66-69)
    - core/accounts.py (`account_for(strategy)` returns the account name string; used to populate account_name)
    - core/motherduck_logger.py (the class built in Plan 01 — constructor signature, methods snapshot_positions/snapshot_portfolio/update_fill)
    - .planning/phases/01-schema-logger-integration/01-PATTERNS.md (runner.py edit section lines 200-275: exact replacement blocks for imports, OrderManager construction, and the try/finally snapshot+fill-poll)
    - .planning/phases/01-schema-logger-integration/01-RESEARCH.md (Pattern 3, Pitfall 1 run_cron blocking / try-finally rationale, Code Examples "Snapshot + fill poll with try/finally", Open Question 2 pnl=None for Phase 1)
    - .planning/phases/01-schema-logger-integration/01-01-SUMMARY.md (the actual MotherDuckLogger constructor signature recorded by Plan 01)
  </read_first>
  <action>
    Edit runner.py only. Add `import os` to the stdlib import group at the top (alongside argparse/importlib/sys). Do NOT add a top-level import of MotherDuckLogger — import it conditionally inside main() so the module has no side effects when the token is absent (SCHEMA-10 / INTEG-03).

    Replace the OrderManager construction block (currently lines 54-56) so it reads, in order:
    - `logger = get_logger(args.strategy)`
    - `client = AlpacaClient(mode=args.mode)`
    - `account_name = account_for(args.strategy)`
    - `token = os.environ.get("MOTHERDUCK_TOKEN")`
    - `md_logger = None`
    - `if token:` then `from core.motherduck_logger import MotherDuckLogger` and `md_logger = MotherDuckLogger(token)` (use the exact constructor signature recorded in 01-01-SUMMARY.md; if it is keyword-only the call becomes `MotherDuckLogger(token=token)`)
    - `order_manager = OrderManager(client=client, logger=logger, md_logger=md_logger, strategy_name=args.strategy, account_name=account_name)` (INTEG-03)

    Wrap the existing cron/stream dispatch (lines 66-69) in `try:` / `finally:` (INTEG-04, INTEG-05). The `try` body keeps the existing `if args.trigger == "cron": run_cron(...) else: run_stream(...)`. The `finally` body runs only `if md_logger:` and then:
    - `positions = client.trading.get_all_positions()` then `md_logger.snapshot_positions(positions, args.strategy, account_name)` (INTEG-04)
    - `account = client.trading.get_account()` then `md_logger.snapshot_portfolio(account, args.strategy, account_name)` (INTEG-04)
    - Fill poll (INTEG-05): import `GetOrdersRequest` from `alpaca.trading.requests`, `QueryOrderStatus` from `alpaca.trading.enums`, and `datetime as dt` locally inside the finally block; build `today = dt.date.today()`; call `client.trading.get_orders(GetOrdersRequest(status=QueryOrderStatus.CLOSED, after=dt.datetime.combine(today, dt.time.min, tzinfo=dt.timezone.utc)))`; for each order `o` where `o.filled_at and o.filled_avg_price`, call `md_logger.update_fill(str(o.id), o.filled_at, float(o.filled_avg_price), None)`.

    Per RESEARCH.md Open Question 2, pass `pnl=None` to update_fill for Phase 1 — realized P&L is computed by the Phase 2 aggregation Flight, not here. Document this with a one-line comment.

    Do not modify scheduler.py or any strategies/ file (INTEG-06). The snapshot block runs at process exit because run_cron blocks (Pitfall 1) — this is expected and correct.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import ast; ast.parse(open('runner.py').read()); print('parse-ok')" && grep -q 'MOTHERDUCK_TOKEN' runner.py && grep -q 'finally:' runner.py && grep -q 'snapshot_positions' runner.py && grep -q 'snapshot_portfolio' runner.py && grep -q 'update_fill' runner.py && grep -q 'GetOrdersRequest' runner.py && test -z "$(git diff --name-only HEAD -- strategies/ core/scheduler.py)"</automated>
  </verify>
  <acceptance_criteria>
    - `runner.py` parses cleanly (`python -c "import ast; ast.parse(open('runner.py').read())"` exits 0)
    - `grep -q 'os.environ.get("MOTHERDUCK_TOKEN")' runner.py` succeeds and MotherDuckLogger is imported conditionally inside `if token:` (no top-level import)
    - `grep -q 'finally:' runner.py` succeeds; the finally block guards on `if md_logger:` before any snapshot/poll call
    - `runner.py` calls `md_logger.snapshot_positions`, `md_logger.snapshot_portfolio`, and `md_logger.update_fill`
    - Fill poll uses `GetOrdersRequest` with `QueryOrderStatus.CLOSED` and passes `pnl=None` to update_fill (Phase 1 — P&L deferred to Phase 2)
    - `git diff --name-only HEAD -- strategies/ core/scheduler.py` is empty (INTEG-06)
    - Running `runner.py --help` (or import of runner) without MOTHERDUCK_TOKEN raises no MotherDuck-related error (SCHEMA-10 preserved end to end)
  </acceptance_criteria>
  <done>runner.py constructs MotherDuckLogger only when the token is present, passes it plus strategy/account to OrderManager, and snapshots positions/portfolio + polls fills in a try/finally — with strategies/ and scheduler.py untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → process | `MOTHERDUCK_TOKEN` read via `os.environ.get` in runner.py; logger constructed only when present |
| Alpaca SDK → MotherDuckLogger | Order/Position/Account data crosses from the external broker API into logged rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Information Disclosure | runner.py token read | mitigate | Token read only via `os.environ.get("MOTHERDUCK_TOKEN")`; never logged or passed to any log call. (ASVS V2) |
| T-01-05 | Denial of Service | try/finally snapshot block | accept | Snapshot/fill-poll runs at process exit; a failing Alpaca call in finally surfaces on shutdown only and does not affect trading. Logged via existing logger. |
| T-01-06 | Tampering | strategy file integrity | mitigate | Integration confined to core/ and runner.py; INTEG-06 verified by `git diff --name-only HEAD -- strategies/` being empty in acceptance criteria. |
| T-01-SC | Tampering | package installs | accept | No new package installs in this plan (duckdb pinned and installed in Plan 01). |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/ -q` — full suite green (new logging tests + existing live-execution regression tests)
- `git diff --name-only HEAD -- strategies/ core/scheduler.py` — empty (INTEG-06: no strategy or scheduler file touched)
- `runner.py` imports/parses without MOTHERDUCK_TOKEN and constructs no logger (SCHEMA-10 end-to-end)
</verification>

<success_criteria>
- OrderManager logs orders from buy/sell/short_sell/buy_to_cover (4 call sites), excludes close_position, backward compatible (INTEG-01, INTEG-02)
- runner.py constructs MotherDuckLogger only when MOTHERDUCK_TOKEN present and passes it + strategy/account to OrderManager (INTEG-03)
- runner.py snapshots positions + portfolio and polls fills via try/finally after run_cron (INTEG-04, INTEG-05)
- No file under strategies/ modified (INTEG-06)
- Running without MOTHERDUCK_TOKEN behaves exactly as before (ROADMAP success criterion 3, SCHEMA-10)
</success_criteria>

<output>
Create `.planning/phases/01-schema-logger-integration/01-02-SUMMARY.md` when done. Record: the 4 log_order insertion points, the runner.py construction signature actually used for MotherDuckLogger, and confirmation that strategies/ and scheduler.py diffs are empty.
</output>
