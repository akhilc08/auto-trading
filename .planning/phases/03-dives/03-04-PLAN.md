---
phase: 03-dives
plan: 04
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - dives/equity-curve.tsx
autonomous: false
requirements: [DIVES-03]
must_haves:
  truths:
    - "The equity curve Dive shows per-strategy cumulative P&L over a 90-day window as a recharts line chart"
    - "There are no gaps in the time axis — days with no trades show a continuous line (COALESCE to last/zero), not a hole"
    - "Each strategy is a distinct line built by pivoting long-format SQL rows to wide format in React"
    - "The Dive renders 'No data yet' on empty daily_pnl rather than crashing"
  artifacts:
    - path: "dives/equity-curve.tsx"
      provides: "DIVES-03 equity curve Dive: generate_series gap-fill SQL + recharts multi-line LineChart with useMemo long->wide pivot"
      contains: "LineChart"
      min_lines: 60
  key_links:
    - from: "dives/equity-curve.tsx"
      to: "trading.main.daily_pnl"
      via: "useSQLQuery generate_series date-spine CROSS JOIN strategies LEFT JOIN daily_pnl"
      pattern: "\"trading\"\\.\"main\"\\.\"daily_pnl\""
    - from: "dives/equity-curve.tsx"
      to: "recharts LineChart"
      via: "useMemo pivots long-format rows to wide; one <Line> per strategy"
      pattern: "useMemo"
---

<objective>
Author the DIVES-03 equity curve Dive: `dives/equity-curve.tsx`, created/saved as a live Dive via the
MotherDuck MCP tool. It uses `useSQLQuery` to read `"trading"."main"."daily_pnl"` through the
`generate_series` date-spine gap-fill query (89-day spine CROSS JOIN distinct strategies LEFT JOIN
cumulative per-strategy P&L, COALESCE to 0), then pivots the long-format rows to wide format in a
`useMemo` and renders a recharts multi-line `LineChart` inside a `ResponsiveContainer` — one line per
strategy, using the `LINE_COLORS` palette.

Runs in Wave 2, parallel with Plans 02/03/05 (no file overlap). Depends on Plan 01 for shared conventions
(`N()`, `LINE_COLORS`, empty-state, table-name format). This is the most involved Dive (gap-fill SQL +
long→wide pivot + recharts) — research rates it LOW-MEDIUM.

Purpose: Visualize per-strategy cumulative P&L over time with a continuous, gap-free line chart (DIVES-03).
Output: `dives/equity-curve.tsx` (committed) + the live `equity-curve` Dive in MotherDuck.
</objective>

<artifacts_this_phase_produces>
This plan creates:
- **File:** `dives/equity-curve.tsx` (DIVES-03)
- **Live Dive:** `equity-curve` in MotherDuck (via MCP `create_dive`/`save_dive`)
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-dives/03-RESEARCH.md
@.planning/phases/03-dives/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author dives/equity-curve.tsx (DIVES-03) and create the live Dive via MCP</name>
  <files>dives/equity-curve.tsx</files>
  <read_first>
    - dives/_conventions.tsx (Plan 01 — copy the N() helper, LINE_COLORS palette, the data-is-rows guard, and the empty-state snippet)
    - dives/README.md (Plan 01 — deliverable model: commit the .tsx AND create via MCP create_dive; REQUIRED_DATABASES fallback)
    - .planning/phases/03-dives/03-RESEARCH.md (Pattern 3 generate_series gap-fill — the FULL SQL and the useMemo long->wide pivot + recharts LineChart code are here; Pitfall 3 COALESCE on LEFT JOIN; Pitfall 7 recharts needs wide-format data)
    - .planning/phases/02-flights/02-04-aggregation-flight-PLAN.md (UPSTREAM — the exact daily_pnl columns DIVES-03 reads: date, strategy_name, account_name, realized_pnl, trade_count, win_count, sharpe_7d, max_drawdown; this Dive uses date, strategy_name, realized_pnl)
  </read_first>
  <action>
    Create `dives/equity-curve.tsx` following 03-RESEARCH.md Pattern 3 exactly (the full SQL, the useMemo pivot, and
    the recharts render are all in research — adapt them). Required specifics:
    - Import `useMemo` from react, `useSQLQuery` from `@motherduck/react-sql-query`, and from recharts: `LineChart`,
      `Line`, `XAxis`, `YAxis`, `CartesianGrid`, `Tooltip`, `Legend`, `ResponsiveContainer`. Inline the `N()` helper
      and `LINE_COLORS` palette copied from `dives/_conventions.tsx`.
    - `useSQLQuery` body = the DIVES-03 gap-fill SQL from 03-RESEARCH.md Pattern 3:
      a `date_spine` CTE using `unnest(generate_series(current_date - INTERVAL 89 DAY, current_date, INTERVAL 1 DAY))::DATE`;
      a `strategy_daily` CTE computing per-strategy cumulative realized_pnl via a window
      `SUM(realized_pnl) OVER (PARTITION BY strategy_name ORDER BY date ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW)`
      from `"trading"."main"."daily_pnl"` (last 89 days); a `strategies` CTE `SELECT DISTINCT strategy_name`; a
      `spine_cross` CROSS JOIN of date_spine x strategies; final SELECT joins spine_cross LEFT JOIN strategy_daily on
      (date, strategy_name) with `COALESCE(sd.cumulative_pnl, 0) AS cumulative_pnl` and
      `strftime(sc.trade_date, '%Y-%m-%d') AS trade_date`, ORDER BY trade_date, strategy_name. The COALESCE is what
      eliminates gaps (Pitfall 3).
    - `const rows = Array.isArray(data) ? data : []`. Branches: isLoading → loading; isError → error;
      `rows.length === 0` → `<p>No data yet — run a strategy to populate.</p>` (empty-state requirement).
    - Pivot long→wide in `useMemo` (Pitfall 7): build `byDate[date][strategy_name] = N(cumulative_pnl)` keyed objects,
      collect the distinct strategy set, return `{ data: sorted-by-date array, strategies: array }`. Memo deps `[rows]`.
    - Render inside `<ResponsiveContainer width="100%" height={300}>` a `<LineChart data={chartData.data}>` with
      `CartesianGrid`, `XAxis dataKey="trade_date"`, `YAxis`, `Tooltip`, `Legend`, and one `<Line key={s} type="monotone"
      dataKey={s} stroke={LINE_COLORS[i % LINE_COLORS.length]} strokeWidth={2} dot={false} />` per strategy in
      `chartData.strategies`.
    - OPTIONAL (research Open Question #3, UX-only): default to top-5 strategies by total P&L with a useDiveState
      "show all" toggle. This is a nice-to-have, NOT required for DIVES-03 correctness — only add it if it does not risk
      the gap-free / multi-line requirements. If added, the toggle is a boolean useDiveState, not a SQL-injected value.
    - REQUIRED_DATABASES: omit per Plan 01 unless Wave 0 recorded a Catalog Error; if needed, add the single line export.

    Then create the Dive live via the MotherDuck MCP `create_dive` tool (named `equity-curve`), passing this TSX source.
    If a query fails with a missing-catalog error, add the REQUIRED_DATABASES one-liner and `save_dive` again.
    Do NOT modify any other file; do NOT touch core/ or strategies/.
  </action>
  <verify>
    <automated>test -f dives/equity-curve.tsx && grep -Eq '"trading"\."main"\."daily_pnl"' dives/equity-curve.tsx && grep -q 'generate_series' dives/equity-curve.tsx && grep -q 'COALESCE' dives/equity-curve.tsx && grep -q 'useMemo' dives/equity-curve.tsx && grep -q 'LineChart' dives/equity-curve.tsx && grep -q 'ResponsiveContainer' dives/equity-curve.tsx && grep -q 'Array.isArray(data)' dives/equity-curve.tsx && grep -q 'No data yet' dives/equity-curve.tsx && ! grep -Eq 'text-\[#|bg-\[#' dives/equity-curve.tsx && echo PASS</automated>
    <human-check>Via the MotherDuck MCP query tool, run the DIVES-03 generate_series gap-fill SQL body and confirm it returns the full date spine (one row per date x strategy, ~90 dates per strategy) with no SQL error and no NULL cumulative_pnl (COALESCE applied). Then open the live `equity-curve` Dive: confirm the line chart has one line per strategy with NO gaps — a strategy with no trades on a day shows a flat/continuous line, not a hole. Confirm empty daily_pnl shows "No data yet" not a crash.</human-check>
  </verify>
  <acceptance_criteria>
    - `dives/equity-curve.tsx` exists; uses `useSQLQuery` with the `generate_series`/`unnest` date spine CROSS JOIN strategies LEFT JOIN cumulative-P&L over `"trading"."main"."daily_pnl"`, with `COALESCE(..., 0)` (gap-fill, Pitfall 3).
    - A `useMemo` pivots long-format rows to wide format; the chart renders one `<Line dataKey={strategy}>` per distinct strategy using `LINE_COLORS` (Pitfall 7).
    - The chart is wrapped in `ResponsiveContainer` with a recharts `LineChart`.
    - The DIVES-03 SQL body executes cleanly via the MCP `query` tool and returns no NULL cumulative_pnl (every date x strategy row COALESCEd) — verifying the gap-fill.
    - `rows = Array.isArray(data) ? data : []` guard present; empty state renders "No data yet"; no Tailwind bracket color classes.
    - The live `equity-curve` Dive is created/saved via the MCP tool and opens without runtime error; lines have no gaps.
  </acceptance_criteria>
  <done>dives/equity-curve.tsx authored and the live equity-curve Dive created via MCP; per-strategy cumulative P&L line chart with no time-series gaps (generate_series + COALESCE + useMemo wide pivot), empty-state safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MotherDuck → daily_pnl table | Read-only SELECT over the user's own data; static SQL (no user-controlled filter interpolated) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-02 | Information Disclosure | Read query over daily_pnl | accept | Read-only over the user's own MotherDuck DB; static SQL, no user input interpolated; LOW |
| T-3-04 | Tampering | BigInt/DECIMAL cumulative_pnl rendered raw crashes the chart | mitigate | All numeric values wrapped with `N()` during the useMemo pivot (Pitfall 4); empty-state guard prevents crash on empty table |
| T-3-05 | Availability/Correctness | Missing COALESCE produces broken (gapped) line | mitigate | `COALESCE(cumulative_pnl, 0)` on the LEFT JOIN (Pitfall 3); verified via MCP query (no NULL rows) and visual no-gap check |
</threat_model>

<verification>
- dives/equity-curve.tsx queries "trading"."main"."daily_pnl" via the generate_series date-spine gap-fill SQL with COALESCE.
- A useMemo pivots long->wide and renders one recharts Line per strategy with LINE_COLORS in a ResponsiveContainer.
- The DIVES-03 SQL body runs cleanly via MCP query and returns no NULL cumulative_pnl (gap-free).
- The live Dive opens; lines have no gaps; empty daily_pnl renders "No data yet".
</verification>

<success_criteria>
- Equity curve Dive shows per-strategy cumulative P&L with no time-series gaps (DIVES-03, ROADMAP success criterion 3).
- 90-day default window via the generate_series spine.
- Empty-state path verified.
</success_criteria>

<output>
Create `.planning/phases/03-dives/03-04-SUMMARY.md` when done. Record: the live Dive name, whether REQUIRED_DATABASES was needed, the DIVES-03 SQL row count (date x strategy) at verification, whether the optional top-5/show-all toggle was added, and confirmation the gap-fill COALESCE eliminates holes.
</output>
