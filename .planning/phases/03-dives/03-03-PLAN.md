---
phase: 03-dives
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - dives/live-positions.tsx
autonomous: false
requirements: [DIVES-02]
must_haves:
  truths:
    - "The live positions Dive shows the latest positions snapshot per strategy with unrealized P&L"
    - "Positive unrealized_pnl renders green (#2d7a00); negative renders red (#bc1200)"
    - "The Dive renders 'No data yet' on empty positions rather than crashing"
  artifacts:
    - path: "dives/live-positions.tsx"
      provides: "DIVES-02 live positions Dive: latest snapshot per strategy, color-coded unrealized P&L"
      contains: "unrealized_pnl"
      min_lines: 35
  key_links:
    - from: "dives/live-positions.tsx"
      to: "trading.main.positions"
      via: "useSQLQuery latest-snapshot-per-strategy SELECT"
      pattern: "\"trading\"\\.\"main\"\\.\"positions\""
    - from: "dives/live-positions.tsx"
      to: "unrealized P&L color coding"
      via: "inline style green/red on the unrealized_pnl cell"
      pattern: "#2d7a00"
---

<objective>
Author the DIVES-02 live positions Dive: `dives/live-positions.tsx`, created/saved as a live Dive via
the MotherDuck MCP tool. It uses `useSQLQuery` to read the LATEST `"trading"."main"."positions"` snapshot
per strategy (via the MAX(snapshot_at) join in 03-RESEARCH.md), renders symbol / strategy / qty /
avg_entry_price / current_price / unrealized_pnl, and color-codes the unrealized_pnl cell green when
positive and red when negative using the exact hex from DIVES-02.

Runs in Wave 2, parallel with Plans 02/04/05 (no file overlap). Depends on Plan 01 for the shared
conventions (`N()`, color hex `#2d7a00`/`#bc1200`, empty-state, table-name format).

Purpose: Make current open positions and their unrealized P&L visible at a glance (DIVES-02).
Output: `dives/live-positions.tsx` (committed) + the live `live-positions` Dive in MotherDuck.
</objective>

<artifacts_this_phase_produces>
This plan creates:
- **File:** `dives/live-positions.tsx` (DIVES-02)
- **Live Dive:** `live-positions` in MotherDuck (via MCP `create_dive`/`save_dive`)
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-dives/03-RESEARCH.md
@.planning/phases/03-dives/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author dives/live-positions.tsx (DIVES-02) and create the live Dive via MCP</name>
  <files>dives/live-positions.tsx</files>
  <read_first>
    - dives/_conventions.tsx (Plan 01 — copy the N() helper, PNL_GREEN/PNL_RED constants, the data-is-rows guard, and the empty-state snippet)
    - dives/README.md (Plan 01 — deliverable model: commit the .tsx AND create via MCP create_dive; REQUIRED_DATABASES fallback)
    - .planning/phases/03-dives/03-RESEARCH.md (Pattern 2 color-coded P&L; SQL Reference → DIVES-02 latest-snapshot-per-strategy query; Pitfall 2 Tailwind brackets fail; Pitfall 4 BigInt guard)
    - .planning/phases/01-schema-logger-integration/01-01-PLAN.md (UPSTREAM — exact positions columns: snapshot_at TIMESTAMPTZ, strategy_name, account_name, symbol, qty, avg_entry_price, current_price, unrealized_pnl — note the COLUMN is unrealized_pnl, not the Alpaca SDK's unrealized_pl)
  </read_first>
  <action>
    Create `dives/live-positions.tsx` following 03-RESEARCH.md Pattern 2 + the DIVES-02 SQL Reference query.
    Required specifics:
    - Import `useSQLQuery` from `@motherduck/react-sql-query`. Inline the `N()` helper and `PNL_GREEN`/`PNL_RED`
      constants copied from `dives/_conventions.tsx`.
    - `useSQLQuery` body = the DIVES-02 query from 03-RESEARCH.md SQL Reference: a `latest_snapshot` CTE that picks
      `MAX(snapshot_at)` per `strategy_name` from `"trading"."main"."positions"`, joined back to positions on
      `strategy_name` AND `snapshot_at = latest_at`. SELECT symbol, strategy_name, qty,
      `ROUND(avg_entry_price, 4) AS avg_entry_price`, `ROUND(current_price, 4) AS current_price`,
      `ROUND(unrealized_pnl, 2) AS unrealized_pnl`, `strftime(snapshot_at::TIMESTAMP, '%Y-%m-%d %H:%M') AS
      snapshot_at`. ORDER BY strategy_name, unrealized_pnl DESC. Use the exact column name `unrealized_pnl` (the
      table column per SCHEMA-02 / Plan 01 — NOT the Alpaca SDK field `unrealized_pl`).
    - `const rows = Array.isArray(data) ? data : []`. Branches: isLoading → loading; isError → error;
      `rows.length === 0` → `<p>No data yet — run a strategy to populate.</p>` (empty-state requirement).
    - Table columns: Symbol, Strategy, Qty, Avg Entry, Current, Unrealized P&L, Snapshot. Wrap every numeric cell with
      `N(...)`. The unrealized_pnl cell uses `style={{ color: N(r.unrealized_pnl) >= 0 ? PNL_GREEN : PNL_RED }}` —
      inline style only, NEVER Tailwind bracket classes (Pitfall 2).
    - REQUIRED_DATABASES: omit per Plan 01 unless Wave 0 recorded a Catalog Error; if needed, add the single line export.

    Then create the Dive live via the MotherDuck MCP `create_dive` tool (named `live-positions`), passing this TSX
    source. If a query fails with a missing-catalog error, add the REQUIRED_DATABASES one-liner and `save_dive` again.
    Do NOT modify any other file; do NOT touch core/ or strategies/.
  </action>
  <verify>
    <automated>test -f dives/live-positions.tsx && grep -Eq '"trading"\."main"\."positions"' dives/live-positions.tsx && grep -q 'MAX(snapshot_at)' dives/live-positions.tsx && grep -q 'unrealized_pnl' dives/live-positions.tsx && grep -q '#2d7a00' dives/live-positions.tsx && grep -q '#bc1200' dives/live-positions.tsx && grep -q 'Array.isArray(data)' dives/live-positions.tsx && grep -q 'No data yet' dives/live-positions.tsx && ! grep -Eq 'text-\[#|bg-\[#' dives/live-positions.tsx && echo PASS</automated>
    <human-check>Via the MotherDuck MCP query tool, run the DIVES-02 latest-snapshot-per-strategy SQL body and confirm it executes with no SQL error. Then open the live `live-positions` Dive: confirm positive unrealized P&L cells render green (#2d7a00) and negative render red (#bc1200), and that only the latest snapshot per strategy appears. Confirm empty positions shows "No data yet" not a crash.</human-check>
  </verify>
  <acceptance_criteria>
    - `dives/live-positions.tsx` exists; uses `useSQLQuery` with the latest-snapshot-per-strategy CTE over `"trading"."main"."positions"` (MAX(snapshot_at) join).
    - SELECT includes symbol, strategy_name, qty, avg_entry_price, current_price, unrealized_pnl (DIVES-02 columns); the column referenced is `unrealized_pnl` (not `unrealized_pl`).
    - The DIVES-02 SQL body executes cleanly via the MCP `query` tool against the live `trading` DB (zero rows acceptable).
    - unrealized_pnl cell uses `style={{ color: ... PNL_GREEN/PNL_RED }}` (green positive / red negative); no Tailwind bracket color classes present.
    - `rows = Array.isArray(data) ? data : []` guard present; empty state renders "No data yet".
    - The live `live-positions` Dive is created/saved via the MCP tool and opens without runtime error.
  </acceptance_criteria>
  <done>dives/live-positions.tsx authored and the live live-positions Dive created via MCP; latest positions per strategy shown with green/red unrealized P&L, empty-state safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MotherDuck → positions table | Read-only SELECT over the user's own data; no user-controlled SQL input in this Dive (no filter) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-02 | Information Disclosure | Read query over positions | accept | Read-only over the user's own MotherDuck DB; no user input is interpolated (static SQL, no filter); LOW |
| T-3-04 | Tampering | BigInt/DECIMAL rendered raw crashes the Dive | mitigate | All numeric cells wrapped with `N()` per conventions (Pitfall 4); empty-state guard prevents crash on empty table |
</threat_model>

<verification>
- dives/live-positions.tsx queries the latest snapshot per strategy from "trading"."main"."positions" (MAX(snapshot_at) join) and renders the DIVES-02 columns.
- unrealized_pnl is color-coded via inline style #2d7a00 (>=0) / #bc1200 (<0); no Tailwind bracket classes.
- The DIVES-02 SQL body runs cleanly via MCP query; the live Dive opens.
- Empty positions renders "No data yet" rather than crashing.
</verification>

<success_criteria>
- Live positions Dive shows open positions with green/red unrealized P&L (DIVES-02, ROADMAP success criterion 2).
- Latest snapshot per strategy only (no stale rows).
- Empty-state path verified.
</success_criteria>

<output>
Create `.planning/phases/03-dives/03-03-SUMMARY.md` when done. Record: the live Dive name, whether REQUIRED_DATABASES was needed, the DIVES-02 SQL row count at verification, and confirmation the color coding uses the exact hex.
</output>
