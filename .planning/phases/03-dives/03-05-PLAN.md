---
phase: 03-dives
plan: 05
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - dives/strategy-comparison.tsx
autonomous: false
requirements: [DIVES-04]
must_haves:
  truths:
    - "The strategy comparison Dive shows one row per strategy with Sharpe 7d, max drawdown, win rate %, trade count, and total P&L side-by-side"
    - "sharpe_7d and max_drawdown are read straight from daily_pnl (pre-computed upstream), not recomputed in the Dive"
    - "win_rate_pct is computed as 100 * SUM(win_count) / NULLIF(SUM(trade_count), 0) — divide-by-zero safe"
    - "The Dive renders 'No data yet' on empty daily_pnl rather than crashing"
  artifacts:
    - path: "dives/strategy-comparison.tsx"
      provides: "DIVES-04 strategy comparison table: Sharpe, drawdown, win rate %, trade count, total P&L per strategy"
      contains: "win_rate_pct"
      min_lines: 35
  key_links:
    - from: "dives/strategy-comparison.tsx"
      to: "trading.main.daily_pnl"
      via: "useSQLQuery GROUP BY strategy_name metrics SELECT"
      pattern: "\"trading\"\\.\"main\"\\.\"daily_pnl\""
    - from: "dives/strategy-comparison.tsx"
      to: "win rate computation"
      via: "NULLIF divide-by-zero guard on SUM(trade_count)"
      pattern: "NULLIF"
---

<objective>
Author the DIVES-04 strategy comparison Dive: `dives/strategy-comparison.tsx`, created/saved as a live
Dive via the MotherDuck MCP tool. It uses `useSQLQuery` to read `"trading"."main"."daily_pnl"` and
GROUP BY strategy_name to produce one row per strategy with: `sharpe_7d` (AVG of the pre-computed column),
`max_drawdown` (MIN of the pre-computed column), `win_rate_pct` (`100 * SUM(win_count) / NULLIF(SUM(trade_count), 0)`),
`trade_count` (SUM), and `total_pnl` (SUM(realized_pnl)). It renders these as a side-by-side table, ordered
by total P&L. Sharpe and drawdown are READ from daily_pnl (computed upstream by the Phase 2 aggregation
Flight) — the Dive never recomputes them.

Runs in Wave 2, parallel with Plans 02/03/04 (no file overlap). Depends on Plan 01 for shared conventions
(`N()`, color hex, empty-state, table-name format). Research rates this MEDIUM-HIGH (the win-rate ratio and
aggregation correctness are the subtle parts).

Purpose: Let the user compare every strategy's risk/return metrics in one table (DIVES-04).
Output: `dives/strategy-comparison.tsx` (committed) + the live `strategy-comparison` Dive in MotherDuck.
</objective>

<artifacts_this_phase_produces>
This plan creates:
- **File:** `dives/strategy-comparison.tsx` (DIVES-04)
- **Live Dive:** `strategy-comparison` in MotherDuck (via MCP `create_dive`/`save_dive`)
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-dives/03-RESEARCH.md
@.planning/phases/03-dives/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author dives/strategy-comparison.tsx (DIVES-04) and create the live Dive via MCP</name>
  <files>dives/strategy-comparison.tsx</files>
  <read_first>
    - dives/_conventions.tsx (Plan 01 — copy the N() helper, the data-is-rows guard, and the empty-state snippet; optionally PNL_GREEN/PNL_RED for the total_pnl cell)
    - dives/README.md (Plan 01 — deliverable model: commit the .tsx AND create via MCP create_dive; REQUIRED_DATABASES fallback)
    - .planning/phases/03-dives/03-RESEARCH.md (Pattern 4 strategy comparison table — the FULL SQL is here; SQL Reference → DIVES-04 query; the column-sources note: sharpe_7d/max_drawdown are pre-computed, win_rate = SUM(win_count)/NULLIF(SUM(trade_count),0); Open Question #2 AVG ignores NULLs)
    - .planning/phases/02-flights/02-04-aggregation-flight-PLAN.md (UPSTREAM — the exact daily_pnl columns and their meaning: realized_pnl, trade_count, win_count, sharpe_7d, max_drawdown; sharpe_7d/max_drawdown are NULL when the aggregation Flight had insufficient history — the Dive must tolerate NULL)
  </read_first>
  <action>
    Create `dives/strategy-comparison.tsx` following 03-RESEARCH.md Pattern 4 + the DIVES-04 SQL Reference query.
    Required specifics:
    - Import `useSQLQuery` from `@motherduck/react-sql-query`. Inline the `N()` helper copied from `dives/_conventions.tsx`.
    - `useSQLQuery` body = the DIVES-04 query from 03-RESEARCH.md: SELECT strategy_name,
      `ROUND(AVG(sharpe_7d), 3) AS sharpe_7d`, `ROUND(MIN(max_drawdown), 4) AS max_drawdown`,
      `ROUND(100.0 * SUM(win_count) / NULLIF(SUM(trade_count), 0), 1) AS win_rate_pct`,
      `SUM(trade_count) AS trade_count`, `ROUND(SUM(realized_pnl), 2) AS total_pnl`
      FROM `"trading"."main"."daily_pnl"` GROUP BY strategy_name ORDER BY total_pnl DESC.
      The `NULLIF(SUM(trade_count), 0)` is the divide-by-zero guard. sharpe_7d/max_drawdown come straight from the
      pre-computed columns (AVG/MIN over the window — AVG ignores NULL rows per Open Question #2). Do NOT recompute
      Sharpe or drawdown in the Dive.
    - `const rows = Array.isArray(data) ? data : []`. Branches: isLoading → loading; isError → error;
      `rows.length === 0` → `<p>No data yet — run a strategy to populate.</p>` (empty-state requirement).
    - Table columns: Strategy, Sharpe 7d, Max Drawdown, Win Rate %, Trade Count, Total P&L. Wrap every numeric cell with
      `N(...)`. Render NULL metric values gracefully (e.g. show "—" when N(value) is for a NULL sharpe/drawdown — guard
      with `r.sharpe_7d == null ? "—" : N(r.sharpe_7d).toFixed(3)`), since the aggregation Flight writes NULL on
      insufficient history. OPTIONALLY color the total_pnl cell with PNL_GREEN/PNL_RED via inline style (nice-to-have,
      consistent with the other Dives) — never Tailwind bracket classes.
    - REQUIRED_DATABASES: omit per Plan 01 unless Wave 0 recorded a Catalog Error; if needed, add the single line export.

    Then create the Dive live via the MotherDuck MCP `create_dive` tool (named `strategy-comparison`), passing this TSX
    source. If a query fails with a missing-catalog error, add the REQUIRED_DATABASES one-liner and `save_dive` again.
    Do NOT modify any other file; do NOT touch core/ or strategies/.
  </action>
  <verify>
    <automated>test -f dives/strategy-comparison.tsx && grep -Eq '"trading"\."main"\."daily_pnl"' dives/strategy-comparison.tsx && grep -q 'sharpe_7d' dives/strategy-comparison.tsx && grep -q 'max_drawdown' dives/strategy-comparison.tsx && grep -q 'win_rate_pct' dives/strategy-comparison.tsx && grep -q 'NULLIF' dives/strategy-comparison.tsx && grep -q 'GROUP BY strategy_name' dives/strategy-comparison.tsx && grep -q 'Array.isArray(data)' dives/strategy-comparison.tsx && grep -q 'No data yet' dives/strategy-comparison.tsx && ! grep -Eq 'text-\[#|bg-\[#' dives/strategy-comparison.tsx && echo PASS</automated>
    <human-check>Via the MotherDuck MCP query tool, run the DIVES-04 metrics SQL body and confirm it returns one row per strategy with no SQL error (and no divide-by-zero — NULLIF guards it). Cross-check one row: total_pnl = SUM of that strategy's realized_pnl, win_rate_pct = 100*win/trade. Then open the live `strategy-comparison` Dive: confirm every strategy with daily_pnl rows appears with Sharpe / drawdown / win rate / trade count / total P&L; NULL Sharpe/drawdown render as "—" not a crash. Confirm empty daily_pnl shows "No data yet".</human-check>
  </verify>
  <acceptance_criteria>
    - `dives/strategy-comparison.tsx` exists; uses `useSQLQuery` over `"trading"."main"."daily_pnl"` GROUP BY strategy_name ORDER BY total_pnl DESC.
    - SELECT includes `AVG(sharpe_7d)`, `MIN(max_drawdown)`, `100.0 * SUM(win_count) / NULLIF(SUM(trade_count), 0) AS win_rate_pct`, `SUM(trade_count)`, `SUM(realized_pnl) AS total_pnl` (DIVES-04 columns); sharpe_7d/max_drawdown are read, not recomputed.
    - The win-rate computation uses `NULLIF(SUM(trade_count), 0)` (divide-by-zero safe).
    - The DIVES-04 SQL body executes cleanly via the MCP `query` tool against the live `trading` DB, returning one row per strategy (zero strategies acceptable if empty).
    - NULL sharpe_7d/max_drawdown render as a placeholder (e.g. "—"), not a crash or "NaN".
    - `rows = Array.isArray(data) ? data : []` guard present; empty state renders "No data yet"; no Tailwind bracket color classes.
    - The live `strategy-comparison` Dive is created/saved via the MCP tool and opens without runtime error.
  </acceptance_criteria>
  <done>dives/strategy-comparison.tsx authored and the live strategy-comparison Dive created via MCP; one row per strategy with Sharpe/drawdown/win-rate/count/total-P&L read from daily_pnl, NULLIF-safe win rate, NULL-tolerant, empty-state safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MotherDuck → daily_pnl table | Read-only SELECT over the user's own data; static SQL (no user-controlled filter interpolated) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-02 | Information Disclosure | Read query over daily_pnl | accept | Read-only over the user's own MotherDuck DB; static SQL, no user input interpolated; LOW |
| T-3-04 | Tampering | BigInt/DECIMAL metrics rendered raw, or NULL Sharpe crashing toFixed() | mitigate | All numeric cells wrapped with `N()`; NULL sharpe/drawdown guarded to "—" before toFixed (Pitfall 4); empty-state guard prevents crash on empty table |
| T-3-06 | Correctness | Divide-by-zero in win rate for a strategy with 0 trades | mitigate | `NULLIF(SUM(trade_count), 0)` guard (research Pattern 4); verified via MCP query returning no error |
</threat_model>

<verification>
- dives/strategy-comparison.tsx queries "trading"."main"."daily_pnl" GROUP BY strategy_name with AVG(sharpe_7d), MIN(max_drawdown), NULLIF-guarded win_rate_pct, SUM(trade_count), SUM(realized_pnl).
- sharpe_7d/max_drawdown are read from the pre-computed columns, not recomputed in the Dive.
- The DIVES-04 SQL body runs cleanly via MCP query (one row per strategy, divide-by-zero safe).
- The live Dive opens; NULL metrics render as a placeholder; empty daily_pnl renders "No data yet".
</verification>

<success_criteria>
- Strategy comparison Dive shows Sharpe, drawdown, win rate, trade count, total P&L for all strategies (DIVES-04, ROADMAP success criterion 4).
- Win rate is divide-by-zero safe; Sharpe/drawdown read from daily_pnl.
- Empty-state path verified.
</success_criteria>

<output>
Create `.planning/phases/03-dives/03-05-SUMMARY.md` when done. Record: the live Dive name, whether REQUIRED_DATABASES was needed, the DIVES-04 SQL row count (number of strategies) at verification, and confirmation Sharpe/drawdown are read (not recomputed) and the NULLIF win-rate guard is present.
</output>
