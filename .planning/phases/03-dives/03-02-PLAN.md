---
phase: 03-dives
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - dives/trade-log.tsx
autonomous: false
requirements: [DIVES-01]
must_haves:
  truths:
    - "The trade log Dive shows trades with symbol, side, qty, submitted_at, filled_avg_price, pnl over the last 90 days by default"
    - "A strategy dropdown (useDiveState) filters the table; changing it changes the visible row set"
    - "The filter value comes from a closed allow-list — raw user input is never interpolated into SQL"
    - "pnl cells are colored green (#2d7a00) when >= 0 and red (#bc1200) when negative"
    - "The Dive renders 'No data yet' on empty trades rather than crashing"
  artifacts:
    - path: "dives/trade-log.tsx"
      provides: "DIVES-01 trade log Dive: useSQLQuery over trades, useDiveState strategy filter, 90-day default, color-coded pnl"
      contains: "useDiveState"
      min_lines: 40
  key_links:
    - from: "dives/trade-log.tsx"
      to: "trading.main.trades"
      via: "useSQLQuery SELECT over the fully-qualified trades table"
      pattern: "\"trading\"\\.\"main\"\\.\"trades\""
    - from: "dives/trade-log.tsx"
      to: "STRATEGIES allow-list"
      via: "filter dropdown options built only from the closed allow-list"
      pattern: "useDiveState"
---

<objective>
Author the DIVES-01 trade log Dive: `dives/trade-log.tsx`, created/saved as a live Dive via the
MotherDuck MCP tool. It uses `useSQLQuery` to read `"trading"."main"."trades"` for the last 90 days,
renders symbol / side / qty / submitted_at / filled_avg_price / pnl in a table, color-codes the pnl
cell green/red, and provides a `useDiveState` strategy dropdown whose options come ONLY from the closed
`STRATEGIES` allow-list established in Plan 01 (the SQL-injection mitigation, T-3-01).

Runs in Wave 2, parallel with Plans 03/04/05 (no file overlap). Depends on Plan 01 for the shared
conventions (`N()`, color hex, empty-state, table-name format) and the `STRATEGIES` allow-list.

Purpose: Make every trade observable and filterable by strategy (DIVES-01).
Output: `dives/trade-log.tsx` (committed) + the live `trade-log` Dive in MotherDuck.
</objective>

<artifacts_this_phase_produces>
This plan creates:
- **File:** `dives/trade-log.tsx` (DIVES-01)
- **Live Dive:** `trade-log` in MotherDuck (via MCP `create_dive`/`save_dive`)
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-dives/03-RESEARCH.md
@.planning/phases/03-dives/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author dives/trade-log.tsx (DIVES-01) and create the live Dive via MCP</name>
  <files>dives/trade-log.tsx</files>
  <read_first>
    - dives/_conventions.tsx (Plan 01 — copy the N() helper, PNL_GREEN/PNL_RED constants, the STRATEGIES allow-list, the data-is-rows guard, and the empty-state snippet from here)
    - dives/README.md (Plan 01 — deliverable model: commit the .tsx AND create via MCP create_dive; REQUIRED_DATABASES fallback)
    - .planning/phases/03-dives/03-RESEARCH.md (Pattern 1 useDiveState filter — the FULL trade-log component is here; SQL Reference → DIVES-01 query; Pitfall 6 injection; Open Question #1 REQUIRED_DATABASES)
    - .planning/phases/01-schema-logger-integration/01-01-PLAN.md (UPSTREAM — the exact trades columns this Dive queries: order_id, strategy_name, account_name, symbol, side, qty, submitted_at TIMESTAMPTZ, filled_at, filled_avg_price, pnl, status)
  </read_first>
  <action>
    Create `dives/trade-log.tsx` following 03-RESEARCH.md Pattern 1 (the full component is in research — adapt it,
    do not reinvent). Required specifics:
    - Import `useDiveState` and `useSQLQuery` from `@motherduck/react-sql-query`. Copy the `N()` helper and the
      `PNL_GREEN`/`PNL_RED` hex constants from `dives/_conventions.tsx` (inline them into this file — the conventions
      file is a reference, not an import target in the Dive runtime).
    - `const [strategy, setStrategy] = useDiveState("strategy", "all")`.
    - Build the `whereClause` ONLY from the closed allow-list: if `strategy === "all"`, `whereClause = ""`; else, the
      value MUST be validated against the `STRATEGIES` allow-list before use (e.g. `STRATEGIES.includes(strategy)`),
      and only then form `AND strategy_name = '<strategy>'`. If a value is somehow not in the allow-list, fall back to
      `""` (treat as "all"). This is the T-3-01 mitigation — never interpolate an unvalidated value.
    - The dropdown `<select>` renders one `<option value="all">All strategies</option>` plus one `<option>` per entry
      in the `STRATEGIES` allow-list (hardcoded values from core/accounts.py via Plan 01) — never a free-text input.
    - `useSQLQuery` body = the DIVES-01 query from 03-RESEARCH.md SQL Reference: SELECT symbol, side, qty,
      `strftime(submitted_at::TIMESTAMP, '%Y-%m-%d %H:%M') AS submitted_at`, `ROUND(filled_avg_price, 4) AS
      filled_avg_price`, `ROUND(pnl, 2) AS pnl`, strategy_name FROM `"trading"."main"."trades"` WHERE
      `submitted_at >= current_date - INTERVAL 90 DAY` + the `whereClause`, ORDER BY submitted_at DESC, LIMIT 500.
    - `const rows = Array.isArray(data) ? data : []`. Branches: `isLoading` → loading text; `isError` → error text;
      `rows.length === 0` → `<p>No data yet — run a strategy to populate.</p>` (the empty-state requirement).
    - Table columns: Symbol, Side, Qty, Submitted, Fill Price, PnL. Wrap every numeric cell with `N(...)`; the PnL cell
      uses `style={{ color: N(r.pnl) >= 0 ? PNL_GREEN : PNL_RED }}` — inline style only, never Tailwind brackets.
    - REQUIRED_DATABASES: omit per Plan 01's decision unless Wave 0 recorded a Catalog Error; if needed, add the single
      line export. Keep it one line.

    Then create the Dive live via the MotherDuck MCP `create_dive` tool (named `trade-log`), passing this TSX source.
    If a query fails with a missing-catalog error, add the REQUIRED_DATABASES one-liner and `save_dive` again (the
    research Open Question #1 fallback). Do NOT modify any other file; do NOT touch core/ or strategies/.
  </action>
  <verify>
    <automated>test -f dives/trade-log.tsx && grep -q 'useDiveState' dives/trade-log.tsx && grep -Eq '"trading"\."main"\."trades"' dives/trade-log.tsx && grep -q 'INTERVAL 90 DAY' dives/trade-log.tsx && grep -q '#2d7a00' dives/trade-log.tsx && grep -q '#bc1200' dives/trade-log.tsx && grep -q 'Array.isArray(data)' dives/trade-log.tsx && grep -q 'No data yet' dives/trade-log.tsx && ! grep -Eq 'text-\[#|bg-\[#' dives/trade-log.tsx && echo PASS</automated>
    <human-check>Via the MotherDuck MCP query tool, run the DIVES-01 SQL body (the SELECT over "trading"."main"."trades" with the 90-day filter) and confirm it executes with no SQL error. Then open the live `trade-log` Dive: confirm the table renders, the strategy dropdown changes the visible row set, and pnl cells are green/red. Confirm empty trades shows "No data yet" not a crash.</human-check>
  </verify>
  <acceptance_criteria>
    - `dives/trade-log.tsx` exists; uses `useDiveState("strategy", "all")` and `useSQLQuery` over `"trading"."main"."trades"`.
    - The query filters `submitted_at >= current_date - INTERVAL 90 DAY` and SELECTs symbol, side, qty, submitted_at, filled_avg_price, pnl (DIVES-01 columns).
    - The strategy filter value is validated against the `STRATEGIES` allow-list before interpolation; the dropdown options are the hardcoded allow-list + "all" — no free-text input (T-3-01).
    - The DIVES-01 SQL body executes cleanly via the MCP `query` tool against the live `trading` DB (zero rows acceptable).
    - pnl cell uses `style={{ color: ... PNL_GREEN/PNL_RED }}`; no Tailwind bracket color classes present.
    - `rows = Array.isArray(data) ? data : []` guard present; empty state renders "No data yet".
    - The live `trade-log` Dive is created/saved via the MCP tool and opens without runtime error.
  </acceptance_criteria>
  <done>dives/trade-log.tsx authored and the live trade-log Dive created via MCP; 90-day trades shown, filterable by the allow-list strategy dropdown, pnl color-coded, empty-state safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (useDiveState "strategy" URL value) → SQL string | The filter value is user/URL controlled and is interpolated into the WHERE clause |
| MotherDuck → trades table | Read-only SELECT over the user's own data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Tampering (Injection) | DIVES-01 useDiveState strategy filter interpolated into the SQL WHERE clause | mitigate | Filter value validated against the closed `STRATEGIES` allow-list (Plan 01) before use; dropdown only offers hardcoded allow-list values + "all"; non-allow-list values fall back to "all" (no interpolation). 03-RESEARCH.md Pitfall 6 |
| T-3-03 | Denial of Service | Unbounded SELECT over trades | mitigate | Query keeps `LIMIT 500` and the 90-day window from the research SQL |
| T-3-02 | Information Disclosure | Read query over trades | accept | Read-only over the user's own MotherDuck DB; no PII; LOW |
</threat_model>

<verification>
- dives/trade-log.tsx queries "trading"."main"."trades" for the last 90 days with the DIVES-01 columns and renders them in a table.
- The strategy dropdown (useDiveState) is built from the closed STRATEGIES allow-list and its value is validated before interpolation (T-3-01).
- pnl is color-coded via inline style with #2d7a00/#bc1200; no Tailwind bracket classes.
- The DIVES-01 SQL body runs cleanly via MCP query; the live Dive opens and the dropdown filters rows.
- Empty trades renders "No data yet" rather than crashing.
</verification>

<success_criteria>
- Trade log Dive shows 90-day history filterable by strategy (DIVES-01, ROADMAP success criterion 1).
- Strategy filter is injection-safe (closed allow-list, validated value).
- pnl color-coded green/red via inline style.
- Empty-state path verified.
</success_criteria>

<output>
Create `.planning/phases/03-dives/03-02-SUMMARY.md` when done. Record: the live Dive name, whether REQUIRED_DATABASES was needed, the DIVES-01 SQL row count at verification, and confirmation the allow-list filter is wired (T-3-01).
</output>
